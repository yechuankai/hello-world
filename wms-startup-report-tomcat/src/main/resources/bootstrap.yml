spring:
 application:
  name: wms-report
 profiles:
  active: dev
 cloud:
  config:
   profile: ${spring.profiles.active}
   label: master
   uri: http://jbt-config-server/wms-config
#初始化加载时需要创建的文件
logging:
 path: C:\\logs