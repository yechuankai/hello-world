spring:
 application:
  name: wms-file
 profiles:
  active: dev
 cloud:
  config:
   profile: ${spring.profiles.active}
   label: master
#   uri: http://127.0.0.1:8888/wms-config
   uri: http://jbt-config-server/wms-config
#初始化加载时需要创建的文件
logging:
 path: C:\\logs 
    